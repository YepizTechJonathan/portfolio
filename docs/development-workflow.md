# Development and Production Workflow

- `dev` is for development and integration work.
- `main` is production-ready and deploys automatically through GitHub Actions.

Workflow: work on `dev`, open a PR into `main` when ready, and merge to `main` to trigger production deployment.
